# frozen_string_literal: true

source "https://rubygems.org"

gem "rails", "~> 7.2"
gem "puma"

# Official Anthropic Ruby SDK (1.x — requires Ruby >= 3.2; this example needs 3.3+).
gem "anthropic", "~> 1.0"

# Loads .env into ENV early in Rails boot.
gem "dotenv-rails"

# Used by lib/three.rb; not a default gem from Ruby 3.4 on.
gem "base64"
